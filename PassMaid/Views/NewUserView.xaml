<UserControl x:Class="PassMaid.Views.NewUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PassMaid.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel HorizontalAlignment="Center">
        <TextBlock Text="New Account" FontSize="30"  HorizontalAlignment="Center" Margin="25 25 25 50"/>

        <TextBlock Text="Username:" VerticalAlignment="Center"/>
        <TextBox Text="{Binding UserUsername}" Width="300" Margin="0 0 0 25"/>

        <TextBlock Text="Password:" VerticalAlignment="Center"/>
        <TextBox Text="{Binding UserPassword}" Margin="0 0 0 25"/>

        <TextBlock Text="Confirm Password:" VerticalAlignment="Center"/>
        <TextBox Text="{Binding ConfirmUserPassword}" Margin="0 0 0 50"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Create" Width="75" Margin="5 0"
                    Command="{Binding CreateUserCommand}"/>
            <Button Content="Cancel" Width="75" Margin="5 0"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
