<UserControl x:Class="PassMaid.Views.VaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:local="clr-namespace:PassMaid.Views"
             xmlns:vm="clr-namespace:PassMaid.ViewModels"
             UseLayoutRounding="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side -->
        <ListBox ItemsSource="{Binding Passwords}" Grid.Column="0" HorizontalContentAlignment="Stretch" SelectedItem="{Binding SelectedPassword}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding EditCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="225"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Name}" Grid.Row="0" Grid.Column="0" Margin="10 0"/>
                        <TextBlock Text="{Binding Website}" FontSize="11" Grid.Row="1" Grid.Column="0" Margin="10 0"/>

                        <!--<TextBlock Text="{Binding Username}" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="10 0"/>-->
                        <!--<TextBlock Text="{Binding Password}" Grid.Row="0" Grid.Column="2" Margin="10 0"/>-->

                        <!--<Image Source="/Resources/Images/info.png" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" SnapsToDevicePixels="True" Stretch="None"/>
                        <Image Source="/Resources/Images/user.png" Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" SnapsToDevicePixels="True" Stretch="None"/>
                        <Image Source="/Resources/Images/key.png" Grid.Row="0" Grid.RowSpan="2" Grid.Column="3" SnapsToDevicePixels="True" Stretch="None"/>-->
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Right Side -->
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Name:"/>
            <TextBox Width="250" Margin="0 0 0 25"/>

            <TextBlock Text="Website:"/>
            <TextBox Width="250" Margin="0 0 0 25"/>

            <TextBlock Text="Username:"/>
            <TextBox Width="250" Margin="0 0 0 25"/>

            <TextBlock Text="Password:"/>
            <TextBox Width="250" Margin="0 0 0 25"/>
        </StackPanel>
    </Grid>
</UserControl>
